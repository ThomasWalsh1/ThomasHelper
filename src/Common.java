

import java.awt.Dimension;

public class Common {
    static public Dimension GUI_SIZE = new Dimension(1080, 720);

    static public Dimension BANNER_SIZE = new Dimension(GUI_SIZE.width, 50);

    static public Dimension TASKPANEL_SIZE = new Dimension(GUI_SIZE.width, 500);

    static public Dimension ADDTASK_BUTTON_SIZE = new Dimension(200, 100);
}
